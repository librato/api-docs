machine:
  ruby:
    version: 2.1.3
test:
  post:
    - bundle exec middleman build

deployment:
  production:
    branch: master
    commands:
      - sudo pip install awscli
      - aws s3 cp build/ s3://librato-api/ --recursive
  staging:
    branch: legacy
    commands:
      - ./deploy.sh
